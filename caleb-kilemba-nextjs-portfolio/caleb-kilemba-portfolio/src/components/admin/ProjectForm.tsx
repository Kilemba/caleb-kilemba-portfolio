import { saveProject } from "@/actions/admin";
import { ImageField } from "@/components/admin/ImageField";

type P = any;
export function ProjectForm({ project }: { project?: P }) {
  const techs = project?.technologies?.map((t: {name:string}) => t.name).join(", ") || "";
  return <form action={saveProject} className="card grid gap-5 p-6"><input type="hidden" name="id" value={project?.id || ""} /><div className="grid gap-5 md:grid-cols-2"><div><label className="label">Title</label><input className="field" name="title" defaultValue={project?.title} required /></div><div><label className="label">Slug</label><input className="field" name="slug" defaultValue={project?.slug} placeholder="automated-business-reporting-platform" required /></div></div><div><label className="label">Category</label><input className="field" name="category" defaultValue={project?.category} required /></div><div><label className="label">Summary</label><textarea className="field min-h-24" name="summary" defaultValue={project?.summary} required /></div><div><label className="label">Business Problem</label><textarea className="field min-h-32" name="businessProblem" defaultValue={project?.businessProblem} required /></div><div><label className="label">Solution</label><textarea className="field min-h-32" name="solution" defaultValue={project?.solution} required /></div><div><label className="label">Business Impact</label><textarea className="field min-h-32" name="businessImpact" defaultValue={project?.businessImpact} required /></div><div><label className="label">Optional Impact Metric</label><input className="field" name="impactMetric" defaultValue={project?.impactMetric || ""} /></div><div><label className="label">Technologies (comma separated)</label><input className="field" name="technologies" defaultValue={techs} /></div><div><label className="label">Architecture</label><textarea className="field min-h-32" name="architecture" defaultValue={project?.architecture} required /></div><div><label className="label">Full Case Study</label><textarea className="field min-h-64" name="caseStudy" defaultValue={project?.caseStudy} required /></div><ImageField name="coverImage" defaultValue={project?.coverImage} /><div className="grid gap-5 md:grid-cols-2"><div><label className="label">GitHub URL</label><input className="field" name="githubUrl" type="url" defaultValue={project?.githubUrl || ""} /></div><div><label className="label">Live URL</label><input className="field" name="liveUrl" type="url" defaultValue={project?.liveUrl || ""} /></div></div><div><label className="label">Display Order</label><input className="field max-w-40" name="sortOrder" type="number" min="0" defaultValue={project?.sortOrder ?? 0} /></div><div className="flex flex-wrap gap-6"><label className="flex items-center gap-2 font-semibold"><input type="checkbox" name="featured" defaultChecked={project?.featured} /> Featured</label><label className="flex items-center gap-2 font-semibold"><input type="checkbox" name="published" defaultChecked={project?.published} /> Published</label></div><button className="btn btn-primary w-fit" type="submit">Save Project</button></form>;
}
